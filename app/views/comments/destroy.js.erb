$('#comment_area').html("<%= escape_javascript(render('comments/index', chocolates: @chocolates,  chocolate: @choco,  comment: @comment, new_comment: @new_comment) ) %>");


$('#comment-all-<%=@comment.id %>').remove();
